
generator client {
    provider        = "prisma-client-js"
    output          = "../generated"
    previewFeatures = []
}

datasource db {
    provider = "postgresql"
    url      = env("PRISMA_DATABASE_URL")
}
